{
  "name": "lloyd-qa-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action for running Lloyd-selected Droid CUA tests.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
